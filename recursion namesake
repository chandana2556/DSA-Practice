#include <iostream>
using namespace std;

void printNumbers(int i, int n) {
    if (i > n) return;   // base case
    cout << i << "\n";   // work
    printNumbers(i + 1, n); // recursive call
}

int main() {
    int n = 10;
    printNumbers(1, n);
    return 0;
}
